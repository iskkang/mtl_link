# Phase 봇 (MTL 도우미) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 봇 아이콘 활성화 → 봇 1:1 대화방 생성 → Anthropic Haiku API 연결로 MTL 도우미 기능 구현

**Architecture:** 클라이언트 트리거 방식 — 사용자가 메시지 전송 시 `useMessages` 훅이 Edge Function `bot-respond`를 호출. 봇 메시지는 Realtime 구독으로 자동 수신. 봇 방 라우팅은 기존 `handleSelectRoom` 재활용 (Approach B).

**Tech Stack:** TypeScript/React, Supabase Edge Functions (Deno), Anthropic Haiku 4.5, Supabase Realtime, Zustand

---

## 파일 변경 지도

### 신규 생성
| 파일 | 역할 |
|------|------|
| `supabase/migrations/20260506000000_bot_user.sql` | is_bot 컬럼, 봇 프로필 시드 데이터 |
| `src/constants/bot.ts` | BOT_USER_ID 상수 |
| `supabase/functions/bot-respond/index.ts` | Anthropic Haiku 호출 Edge Function |

### 수정
| 파일 | 변경 |
|------|------|
| `src/types/database.ts` | profiles Row/Insert/Update에 `is_bot: boolean` 추가 |
| `src/types/chat.ts` | `MessageWithSender.sender` + `RoomListItem.members`에 `is_bot` 추가 |
| `src/stores/messageStore.ts` | MSG_SELECT sender 쿼리에 `is_bot` 추가 |
| `src/services/roomService.ts` | profiles select + 타입 어노테이션에 `is_bot` 추가 |
| `src/pages/ChatPage.tsx` | `handleSectionChange` 추가 (bots 섹션 인터셉트) |
| `src/components/layout/MenuRail.tsx` | bots를 PLACEHOLDER에서 분리, 활성 버튼으로 이동 |
| `src/lib/i18n.ts` | `botTyping` 키 6개 언어 추가 |
| `src/hooks/useMessages.ts` | `isBotTyping` 상태 + 봇 트리거 로직 |
| `src/components/layout/ChatWindow.tsx` | `isBotTyping` 추출 → MessageList에 전달 |
| `src/components/chat/MessageList.tsx` | `isBotTyping` prop + 타이핑 인디케이터 렌더링 |
| `src/components/ui/Avatar.tsx` | `is_bot` prop + Bot 아이콘 |
| `src/components/chat/MessageBubble.tsx` | 봇 시각 구분 (border + AI 뱃지) |

> **MoreSheet.tsx**: 이미 bots 항목 포함, 변경 불필요

---

## Task 1: DB 마이그레이션 + 봇 상수

**Files:**
- Create: `supabase/migrations/20260506000000_bot_user.sql`
- Create: `src/constants/bot.ts`

- [ ] **Step 1: 마이그레이션 파일 작성**

`supabase/migrations/20260506000000_bot_user.sql` 생성:

```sql
-- 1. is_bot 컬럼 추가
ALTER TABLE public.profiles
  ADD COLUMN is_bot boolean NOT NULL DEFAULT false;

-- 2. auto_join_channels 트리거 재정의: 봇 제외 + is_default 조건 유지
CREATE OR REPLACE FUNCTION public.auto_join_channels()
RETURNS TRIGGER AS $$
BEGIN
  IF NEW.is_bot THEN RETURN NEW; END IF;
  INSERT INTO public.room_members (room_id, user_id, role)
  SELECT id, NEW.id, 'member'
  FROM   public.rooms
  WHERE  room_type = 'channel'
    AND  is_default = true
  ON CONFLICT DO NOTHING;
  RETURN NEW;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;

-- 3. auth.users에 봇 row 삽입 (profiles FK 충족)
INSERT INTO auth.users (
  id, email, encrypted_password, role, aud,
  created_at, updated_at, email_confirmed_at
) VALUES (
  '00000000-0000-0000-0000-000000000001',
  'bot@mtl.internal', '', 'authenticated', 'authenticated',
  now(), now(), now()
) ON CONFLICT (id) DO NOTHING;

-- 4. profiles에 봇 row 삽입
INSERT INTO public.profiles (
  id, email, name, is_bot, status, preferred_language, must_change_password
) VALUES (
  '00000000-0000-0000-0000-000000000001',
  'bot@mtl.internal', 'MTL 도우미',
  true, 'active', 'ko', false
) ON CONFLICT (id) DO NOTHING;
```

- [ ] **Step 2: 봇 상수 파일 작성**

`src/constants/bot.ts` 생성:

```ts
export const BOT_USER_ID = '00000000-0000-0000-0000-000000000001'
```

- [ ] **Step 3: 마이그레이션 적용**

```bash
npx supabase db push
```

Expected output: migration applied successfully (no errors)

DB에서 확인:
```sql
SELECT id, name, is_bot FROM profiles WHERE is_bot = true;
-- Expected: 1 row — 00000000..., MTL 도우미, true
```

- [ ] **Step 4: 커밋**

```bash
git add supabase/migrations/20260506000000_bot_user.sql src/constants/bot.ts
git commit -m "feat: add is_bot column and bot profile seed data"
```

---

## Task 2: TypeScript 타입 업데이트

**Files:**
- Modify: `src/types/database.ts`
- Modify: `src/types/chat.ts`

- [ ] **Step 1: database.ts profiles 타입에 is_bot 추가**

`src/types/database.ts`의 `profiles` Row 블록에서 `is_admin: boolean` 다음 줄에 `is_bot: boolean` 추가:

```ts
// Row (line 31 근처):
is_admin:             boolean
is_bot:               boolean   // ← 추가
must_change_password: boolean

// Insert (line 45 근처):
is_admin?:             boolean
is_bot?:               boolean   // ← 추가
must_change_password?: boolean

// Update (line 59 근처):
is_admin?:             boolean
is_bot?:               boolean   // ← 추가
must_change_password?: boolean
```

- [ ] **Step 2: chat.ts MessageWithSender.sender에 is_bot 추가**

`src/types/chat.ts` line 21:

현재:
```ts
sender:        Pick<Profile, 'id' | 'name' | 'avatar_url'> | null
```

변경:
```ts
sender:        Pick<Profile, 'id' | 'name' | 'avatar_url' | 'is_bot'> | null
```

- [ ] **Step 3: chat.ts RoomListItem.members에 is_bot 추가**

`src/types/chat.ts` line 38:

현재:
```ts
members:      (Pick<Profile, 'id' | 'name' | 'avatar_url' | 'preferred_language'> & { last_read_at: string | null })[]
```

변경:
```ts
members:      (Pick<Profile, 'id' | 'name' | 'avatar_url' | 'preferred_language' | 'is_bot'> & { last_read_at: string | null })[]
```

- [ ] **Step 4: 빌드 타입 검증**

```bash
npm run build
```

Expected: 컴파일 오류 발생 — messageStore.ts, roomService.ts 등 is_bot 미포함 쿼리에서 타입 에러. 다음 Task에서 수정.

- [ ] **Step 5: 커밋**

```bash
git add src/types/database.ts src/types/chat.ts
git commit -m "feat: add is_bot field to profiles and related types"
```

---

## Task 3: 데이터 레이어 업데이트

**Files:**
- Modify: `src/stores/messageStore.ts`
- Modify: `src/services/roomService.ts`

- [ ] **Step 1: messageStore.ts MSG_SELECT sender에 is_bot 추가**

`src/stores/messageStore.ts` line 21:

현재:
```ts
  sender:profiles!sender_id(id, name, avatar_url),
```

변경:
```ts
  sender:profiles!sender_id(id, name, avatar_url, is_bot),
```

- [ ] **Step 2: roomService.ts profiles select에 is_bot 추가**

`src/services/roomService.ts` line 47:

현재:
```ts
    .select('id, name, avatar_url, preferred_language')
```

변경:
```ts
    .select('id, name, avatar_url, preferred_language, is_bot')
```

- [ ] **Step 3: roomService.ts membersByRoom 타입 어노테이션 업데이트**

`src/services/roomService.ts` line 93:

현재:
```ts
  const membersByRoom: Record<string, (Pick<Profile, 'id' | 'name' | 'avatar_url' | 'preferred_language'> & { last_read_at: string | null })[]> = {}
```

변경:
```ts
  const membersByRoom: Record<string, (Pick<Profile, 'id' | 'name' | 'avatar_url' | 'preferred_language' | 'is_bot'> & { last_read_at: string | null })[]> = {}
```

- [ ] **Step 4: 빌드 검증**

```bash
npm run build
```

Expected: 이전 타입 에러가 해소됨. 새 에러가 있다면 수정.

- [ ] **Step 5: 커밋**

```bash
git add src/stores/messageStore.ts src/services/roomService.ts
git commit -m "feat: include is_bot in message sender and room member queries"
```

---

## Task 4: 봇 방 라우팅

**Files:**
- Modify: `src/pages/ChatPage.tsx`
- Modify: `src/components/layout/MenuRail.tsx`

- [ ] **Step 1: ChatPage.tsx에 fetchRooms + BOT_USER_ID import 추가**

`src/pages/ChatPage.tsx` 상단 imports에 추가:

현재:
```ts
import { createDirectRoom }   from '../services/roomService'
```

변경:
```ts
import { createDirectRoom, fetchRooms } from '../services/roomService'
import { BOT_USER_ID } from '../constants/bot'
```

- [ ] **Step 2: handleSectionChange 함수 추가**

`src/pages/ChatPage.tsx`에서 `handleLogoClick` 함수 다음에 추가:

```ts
const handleSectionChange = async (s: Section) => {
  if (s === 'bots') {
    try {
      const roomId = await createDirectRoom(BOT_USER_ID)
      const rooms = await fetchRooms()
      useRoomStore.getState().setRooms(rooms)
      handleSelectRoom(roomId)
    } catch (err) {
      console.error('봇 방 생성 실패:', err)
    }
    return
  }
  setActiveSection(s)
}
```

- [ ] **Step 3: AppLayout의 onSectionChange를 handleSectionChange로 교체**

`src/pages/ChatPage.tsx` JSX에서:

현재:
```tsx
onSectionChange={setActiveSection}
```

변경:
```tsx
onSectionChange={handleSectionChange}
```

- [ ] **Step 4: MenuRail.tsx에서 bots를 PLACEHOLDER에서 분리**

`src/components/layout/MenuRail.tsx`에서 PLACEHOLDER 배열의 `bots` 항목 제거:

현재:
```ts
const PLACEHOLDER: { id: Section; Icon: React.ElementType; label: string }[] = [
  { id: 'announcements', Icon: Bell,       label: t('menuRailAnnouncements') },
  { id: 'calendar',      Icon: Calendar,   label: t('menuRailCalendar')      },
  { id: 'files',         Icon: FolderOpen, label: t('menuRailFiles')         },
  { id: 'channels',      Icon: Hash,       label: t('menuRailChannels')      },
  { id: 'bots',          Icon: Bot,        label: t('menuRailBots')          },
]
```

변경:
```ts
const PLACEHOLDER: { id: Section; Icon: React.ElementType; label: string }[] = [
  { id: 'announcements', Icon: Bell,       label: t('menuRailAnnouncements') },
  { id: 'calendar',      Icon: Calendar,   label: t('menuRailCalendar')      },
  { id: 'files',         Icon: FolderOpen, label: t('menuRailFiles')         },
  { id: 'channels',      Icon: Hash,       label: t('menuRailChannels')      },
]
```

- [ ] **Step 5: MenuRail.tsx에서 구분선과 봇 버튼을 PLACEHOLDER 섹션 위에 추가**

`src/components/layout/MenuRail.tsx`의 JSX에서 PLACEHOLDER 섹션 렌더링 부분을 찾아 수정:

현재 (PLACEHOLDER 섹션 전체):
```tsx
        <div className="flex flex-col items-center gap-0.5 w-full">
          {PLACEHOLDER.map(({ id, Icon, label }) => (
            <RailBtn
              key={id}
              Icon={Icon}
              label={label}
              active={activeSection === id}
              muted
              onClick={() => onSectionChange(id)}
            />
          ))}
        </div>
```

변경:
```tsx
        <div className="flex flex-col items-center gap-0.5 w-full">
          <RailBtn
            Icon={Bot}
            label={t('menuRailBots')}
            active={activeSection === 'chat' && false}
            onClick={() => onSectionChange('bots')}
          />
        </div>

        <div className="w-8 my-1.5 border-t flex-shrink-0" style={{ borderColor: 'var(--side-line)' }} />

        <div className="flex flex-col items-center gap-0.5 w-full">
          {PLACEHOLDER.map(({ id, Icon, label }) => (
            <RailBtn
              key={id}
              Icon={Icon}
              label={label}
              active={activeSection === id}
              muted
              onClick={() => onSectionChange(id)}
            />
          ))}
        </div>
```

> 봇 버튼은 `active` 하이라이트 없음 (클릭하면 chat 섹션으로 이동하므로). `muted` prop 없으므로 기본 opacity=1.

- [ ] **Step 6: 빌드 검증**

```bash
npm run build
```

Expected: 컴파일 성공 (또는 i18n 관련 경고만)

- [ ] **Step 7: 커밋**

```bash
git add src/pages/ChatPage.tsx src/components/layout/MenuRail.tsx src/constants/bot.ts
git commit -m "feat: activate bot icon in MenuRail and add bot room routing"
```

---

## Task 5: i18n — botTyping 키 추가

**Files:**
- Modify: `src/lib/i18n.ts`

- [ ] **Step 1: 각 언어 번역 객체에 botTyping 키 추가**

`src/lib/i18n.ts`에서 각 언어 블록에 `botTyping` 키를 추가. `menuRailBots` 키 근처에 배치.

Korean (`ko`):
```ts
menuRailBots:          '봇',
botTyping:             'MTL 도우미가 입력 중...',
```

English (`en`):
```ts
menuRailBots:          'Bots',
botTyping:             'MTL Assistant is typing...',
```

Russian (`ru`):
```ts
menuRailBots:          'Боты',
botTyping:             'MTL Ассистент печатает...',
```

Uzbek (`uz`):
```ts
menuRailBots:          'Botlar',
botTyping:             'MTL Yordamchi yozmoqda...',
```

Chinese (`zh`):
```ts
menuRailBots:          '机器人',
botTyping:             'MTL助手正在输入...',
```

Japanese (`ja`):
```ts
menuRailBots:          'ボット',
botTyping:             'MTLアシスタントが入力中...',
```

- [ ] **Step 2: 빌드 검증**

```bash
npm run build
```

Expected: 성공

- [ ] **Step 3: 커밋**

```bash
git add src/lib/i18n.ts
git commit -m "feat: add botTyping i18n key for 6 languages"
```

---

## Task 6: Edge Function — bot-respond

**Files:**
- Create: `supabase/functions/bot-respond/index.ts`

- [ ] **Step 1: Edge Function 파일 작성**

`supabase/functions/bot-respond/index.ts` 생성:

```ts
import { createClient } from 'https://esm.sh/@supabase/supabase-js@2'

const CORS = {
  'Access-Control-Allow-Origin':  '*',
  'Access-Control-Allow-Headers': 'authorization, x-client-info, apikey, content-type',
}

const BOT_USER_ID = '00000000-0000-0000-0000-000000000001'

const SYSTEM_PROMPT_TEMPLATE = `You are MTL Assistant, an AI helper for MTL Link — an internal communication platform for a freight forwarding and logistics company.

Your role:
- Answer questions about logistics, freight, customs, shipping, and trade
- Help with task management and internal Q&A
- Assist with understanding company communications

Guidelines:
- Always respond in {userLanguage}
- Be concise and professional, but friendly
- Use industry-standard logistics terminology (B/L, FCL/LCL, freight rates, customs)
- If you don't know something specific to this company, say so clearly
- Never make up specific shipment, client, or internal data

Company context: International freight forwarding company handling FCL/LCL, customs clearance, B/L management, and freight rate negotiations.`

interface BotRequest {
  roomId:       string
  userMessage:  string
  userLanguage: string
}

Deno.serve(async (req: Request) => {
  if (req.method === 'OPTIONS') return new Response('ok', { headers: CORS })

  const json = (body: unknown, status = 200) =>
    new Response(JSON.stringify(body), {
      status,
      headers: { ...CORS, 'Content-Type': 'application/json' },
    })

  try {
    const { roomId, userMessage, userLanguage } = await req.json() as BotRequest

    if (!roomId || !userMessage) return json({ error: 'missing required fields' }, 400)

    const supabaseUrl  = Deno.env.get('SUPABASE_URL')!
    const serviceKey   = Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!
    const anthropicKey = Deno.env.get('ANTHROPIC_API_KEY')

    if (!anthropicKey) {
      console.warn('[bot-respond] ANTHROPIC_API_KEY not set')
      return json({ error: 'Bot service not configured' }, 500)
    }

    const db = createClient(supabaseUrl, serviceKey)

    // 1. 봇 방 검증
    const { data: botMember } = await db
      .from('room_members')
      .select('user_id')
      .eq('room_id', roomId)
      .eq('user_id', BOT_USER_ID)
      .maybeSingle()

    if (!botMember) return json({ error: 'not a bot room' }, 400)

    // 2. Rate limit: 최근 1분 내 봇 응답 ≥ 10
    const { data: recentBotMessages } = await db
      .from('messages')
      .select('id')
      .eq('room_id', roomId)
      .eq('sender_id', BOT_USER_ID)
      .gte('created_at', new Date(Date.now() - 60_000).toISOString())

    if ((recentBotMessages?.length ?? 0) >= 10) {
      return new Response('rate limit', { status: 429 })
    }

    // 3. 최근 10개 메시지 로드 (컨텍스트)
    const { data: recentMsgs } = await db
      .from('messages')
      .select('sender_id, content')
      .eq('room_id', roomId)
      .eq('message_type', 'text')
      .is('deleted_at', null)
      .order('created_at', { ascending: false })
      .limit(10)

    const contextMessages = (recentMsgs ?? [])
      .reverse()
      .filter(m => m.content)
      .map(m => ({
        role: m.sender_id === BOT_USER_ID ? 'assistant' : 'user',
        content: m.content as string,
      }))

    // 현재 메시지가 이미 context에 포함된 경우 중복 방지
    const lastCtx = contextMessages[contextMessages.length - 1]
    if (!lastCtx || lastCtx.role !== 'user' || lastCtx.content !== userMessage) {
      contextMessages.push({ role: 'user', content: userMessage })
    }

    // 4. Anthropic Haiku 호출
    const systemPrompt = SYSTEM_PROMPT_TEMPLATE.replace('{userLanguage}', userLanguage)

    const claudeRes = await fetch('https://api.anthropic.com/v1/messages', {
      method: 'POST',
      headers: {
        'x-api-key':         anthropicKey,
        'anthropic-version': '2023-06-01',
        'content-type':      'application/json',
      },
      body: JSON.stringify({
        model:      'claude-haiku-4-5-20251001',
        max_tokens: 800,
        system:     systemPrompt,
        messages:   contextMessages,
      }),
    })

    if (!claudeRes.ok) {
      const errText = await claudeRes.text()
      throw new Error(`Anthropic API ${claudeRes.status}: ${errText}`)
    }

    const claudeData = await claudeRes.json() as {
      content?: { type: string; text: string }[]
    }
    const botReply = claudeData.content?.[0]?.text?.trim()
    if (!botReply) throw new Error('Empty response from Anthropic')

    // 5. 봇 메시지 INSERT
    const { error: insertError } = await db.from('messages').insert({
      room_id:         roomId,
      sender_id:       BOT_USER_ID,
      content:         botReply,
      message_type:    'text',
      source_language: userLanguage,
    })

    if (insertError) throw insertError

    console.info(`[bot-respond] responded in ${userLanguage}, room=${roomId}`)
    return json({ ok: true })

  } catch (err) {
    const msg = err instanceof Error ? err.message : String(err)
    console.error('[bot-respond]', msg)
    return json({ error: msg }, 500)
  }
})
```

- [ ] **Step 2: Anthropic API 키 시크릿 설정**

```bash
npx supabase secrets set ANTHROPIC_API_KEY=sk-ant-...
```

(실제 키 값으로 교체)

- [ ] **Step 3: Edge Function 배포**

```bash
npx supabase functions deploy bot-respond
```

Expected: `Deployed bot-respond` 출력

- [ ] **Step 4: 커밋**

```bash
git add supabase/functions/bot-respond/index.ts
git commit -m "feat: add bot-respond Edge Function with Haiku 4.5"
```

---

## Task 7: useMessages 봇 트리거 + ChatWindow 연결

**Files:**
- Modify: `src/hooks/useMessages.ts`
- Modify: `src/components/layout/ChatWindow.tsx`

- [ ] **Step 1: useMessages.ts에 필요한 import 추가**

`src/hooks/useMessages.ts` 상단에 추가:

현재:
```ts
import { useState } from 'react'  // 없음 — useEffect만 있음
```

변경 (기존 import에 `useState` 추가):
```ts
import { useEffect, useState } from 'react'
```

`useRoomStore` import 추가:

현재:
```ts
import { useRoomStore } from '../stores/roomStore'
```

(이미 있음 — 확인만)

`BOT_USER_ID` import 추가:
```ts
import { BOT_USER_ID } from '../constants/bot'
```

- [ ] **Step 2: useMessages 함수 본문 수정**

`src/hooks/useMessages.ts`의 `useMessages` 함수에서:

**isBotRoom 상태 + isBotTyping 상태 추가** (useEffect 바로 위에):
```ts
const room = useRoomStore(s => s.rooms.find(r => r.id === roomId) ?? null)
const isBotRoom = room?.members.some(m => m.is_bot) ?? false
const [isBotTyping, setIsBotTyping] = useState(false)
```

**send 함수를 async 래퍼로 교체** (기존 return 블록에서):

현재:
```ts
return {
  messages: roomId ? (store.messagesByRoom[roomId] ?? []) : [],
  loading:  roomId ? (store.loadingByRoom[roomId] ?? false) : false,
  hasMore:  roomId ? (store.hasMoreByRoom[roomId] ?? false) : false,
  send: (content: string, replyToId?: string | null, replyMessage?: ReplyRef | null, needsResponse?: boolean, mentions?: string[]) =>
    sendTextMessage(roomId!, content, profile?.preferred_language, replyToId, replyMessage, needsResponse, undefined, mentions),
  loadMore,
}
```

변경:
```ts
const send = async (
  content: string,
  replyToId?: string | null,
  replyMessage?: ReplyRef | null,
  needsResponse?: boolean,
  mentions?: string[],
) => {
  await sendTextMessage(roomId!, content, profile?.preferred_language, replyToId, replyMessage, needsResponse, undefined, mentions)
  if (isBotRoom) {
    setIsBotTyping(true)
    try {
      await supabase.functions.invoke('bot-respond', {
        body: {
          roomId,
          userMessage:  content,
          userLanguage: profile?.preferred_language ?? 'ko',
        },
      })
    } finally {
      setIsBotTyping(false)
    }
  }
}

return {
  messages:    roomId ? (store.messagesByRoom[roomId] ?? []) : [],
  loading:     roomId ? (store.loadingByRoom[roomId] ?? false) : false,
  hasMore:     roomId ? (store.hasMoreByRoom[roomId] ?? false) : false,
  send,
  loadMore,
  isBotTyping,
  isBotRoom,
}
```

- [ ] **Step 3: ChatWindow.tsx에서 isBotTyping 추출 및 MessageList에 전달**

`src/components/layout/ChatWindow.tsx` line 49:

현재:
```ts
const { messages, loading, hasMore, send, loadMore } = useMessages(roomId)
```

변경:
```ts
const { messages, loading, hasMore, send, loadMore, isBotTyping } = useMessages(roomId)
```

`src/components/layout/ChatWindow.tsx`의 `<MessageList ... />` 호출부에 `isBotTyping` prop 추가:

현재 (line 320 근처):
```tsx
<MessageList
  messages={messages}
  loading={loading}
  hasMore={hasMore}
  currentUserId={currentUserId}
  isGroupRoom={isGroup}
  members={room.members}
  onLoadMore={loadMore}
  onOpenThread={setThreadRootId}
  onScrollToMessage={scrollToMessage}
  searchQuery={searchOpen ? searchQuery : ''}
  currentResultId={searchCurrent?.id ?? null}
  targetLanguage={targetLanguage ?? undefined}
  roomId={roomId ?? ''}
/>
```

변경:
```tsx
<MessageList
  messages={messages}
  loading={loading}
  hasMore={hasMore}
  currentUserId={currentUserId}
  isGroupRoom={isGroup}
  members={room.members}
  onLoadMore={loadMore}
  onOpenThread={setThreadRootId}
  onScrollToMessage={scrollToMessage}
  searchQuery={searchOpen ? searchQuery : ''}
  currentResultId={searchCurrent?.id ?? null}
  targetLanguage={targetLanguage ?? undefined}
  roomId={roomId ?? ''}
  isBotTyping={isBotTyping}
/>
```

- [ ] **Step 4: 빌드 검증**

```bash
npm run build
```

Expected: MessageList에 `isBotTyping` prop이 없어 타입 에러. 다음 step에서 수정.

- [ ] **Step 5: 커밋 (타입 에러 수정 후)**

(MessageList 수정 후 함께 커밋 — 다음 Task에서)

---

## Task 8: UI — 타이핑 인디케이터 + 봇 시각 구분

**Files:**
- Modify: `src/components/chat/MessageList.tsx`
- Modify: `src/components/ui/Avatar.tsx`
- Modify: `src/components/chat/MessageBubble.tsx`

- [ ] **Step 1: MessageList.tsx에 isBotTyping prop 추가 및 렌더링**

`src/components/chat/MessageList.tsx` Props 인터페이스에 추가:

현재:
```ts
interface Props {
  messages:          MessageWithSender[]
  loading:           boolean
  hasMore:           boolean
  currentUserId:     string
  isGroupRoom:       boolean
  members:           RoomListItem['members']
  onLoadMore:        () => void
  onOpenThread?:     (messageId: string) => void
  onScrollToMessage: (messageId: string) => void
  searchQuery?:      string
  currentResultId?:  string | null
  targetLanguage?:   string
  roomId?:           string
}
```

변경:
```ts
interface Props {
  messages:          MessageWithSender[]
  loading:           boolean
  hasMore:           boolean
  currentUserId:     string
  isGroupRoom:       boolean
  members:           RoomListItem['members']
  onLoadMore:        () => void
  onOpenThread?:     (messageId: string) => void
  onScrollToMessage: (messageId: string) => void
  searchQuery?:      string
  currentResultId?:  string | null
  targetLanguage?:   string
  roomId?:           string
  isBotTyping?:      boolean
}
```

함수 시그니처에 `isBotTyping = false` 추가:

현재:
```ts
export function MessageList({ messages, loading, hasMore, currentUserId, isGroupRoom, members, onLoadMore, onOpenThread, onScrollToMessage, searchQuery = '', currentResultId = null, targetLanguage, roomId }: Props) {
```

변경:
```ts
export function MessageList({ messages, loading, hasMore, currentUserId, isGroupRoom, members, onLoadMore, onOpenThread, onScrollToMessage, searchQuery = '', currentResultId = null, targetLanguage, roomId, isBotTyping = false }: Props) {
```

`useTranslation` import 확인 (이미 있음). 타이핑 인디케이터를 `<div ref={bottomRef} />` 바로 앞에 추가:

현재:
```tsx
      <div ref={bottomRef} />
    </div>
```

변경:
```tsx
      {isBotTyping && (
        <div className="px-4 py-2 text-sm" style={{ color: 'var(--side-mute)' }}>
          {t('botTyping')}
        </div>
      )}
      <div ref={bottomRef} />
    </div>
```

- [ ] **Step 2: Avatar.tsx에 is_bot prop 추가**

`src/components/ui/Avatar.tsx`에 Bot 아이콘 import 추가:

파일 최상단에:
```ts
import { Bot } from 'lucide-react'
```

Props 인터페이스에 `is_bot` 추가:

현재:
```ts
interface Props {
  name: string
  avatarUrl?: string | null
  size?: 'xs' | 'sm' | 'md' | 'lg'
  className?: string
}
```

변경:
```ts
interface Props {
  name:      string
  avatarUrl?: string | null
  size?:     'xs' | 'sm' | 'md' | 'lg'
  className?: string
  is_bot?:   boolean
}
```

Avatar 함수에서 `is_bot` 처리를 avatarUrl 체크 앞에 추가:

현재:
```ts
export function Avatar({ name, avatarUrl, size = 'md', className = '' }: Props) {
  const sizeClass = SIZES[size]
  const colorClass = colorFor(name)

  if (avatarUrl) {
```

변경:
```ts
export function Avatar({ name, avatarUrl, size = 'md', className = '', is_bot = false }: Props) {
  const sizeClass = SIZES[size]
  const colorClass = colorFor(name)

  if (is_bot) {
    return (
      <div
        className={`${sizeClass} rounded-full flex items-center justify-center flex-shrink-0 ${className}`}
        style={{ background: 'var(--brand)', color: 'white' }}
      >
        <Bot size={size === 'xs' ? 12 : size === 'sm' ? 14 : size === 'lg' ? 20 : 16} />
      </div>
    )
  }

  if (avatarUrl) {
```

- [ ] **Step 3: MessageBubble.tsx에서 봇 아바타 + 시각 구분 적용**

`src/components/chat/MessageBubble.tsx`의 아바타 렌더링 부분 (line 246 근처):

현재:
```tsx
          {!isContinuation && message.sender && (
            <Avatar
              name={message.sender.name}
              avatarUrl={message.sender.avatar_url}
              size="sm"
            />
          )}
```

변경:
```tsx
          {!isContinuation && message.sender && (
            <Avatar
              name={message.sender.name}
              avatarUrl={message.sender.avatar_url}
              size="sm"
              is_bot={message.sender.is_bot ?? false}
            />
          )}
```

봇 메시지 발신자명 표시 부분 (line 259 근처) — "AI" 뱃지 추가:

현재:
```tsx
        {showSenderInfo && !isOwn && !isContinuation && message.sender && (
          <span className="text-[11px] font-semibold mb-1 ml-1" style={{ color: 'var(--brand)' }}>
            {message.sender.name}
          </span>
        )}
```

변경:
```tsx
        {showSenderInfo && !isOwn && !isContinuation && message.sender && (
          <span className="flex items-center gap-1 mb-1 ml-1">
            <span className="text-[11px] font-semibold" style={{ color: 'var(--brand)' }}>
              {message.sender.name}
            </span>
            {message.sender.is_bot && (
              <span
                className="text-[9px] font-bold px-1 py-0.5 rounded"
                style={{ background: 'var(--brand)', color: 'white' }}
              >
                AI
              </span>
            )}
          </span>
        )}
```

봇 메시지 말풍선 좌측 border 추가 (line 278 근처) — isOwn=false 스타일 블록:

현재:
```ts
            } : {
              background: hasSelfMention ? 'rgba(147,51,234,0.08)' : 'var(--bubble-in)',
              border: hasSelfMention ? '1px solid rgba(147,51,234,0.20)' : '1px solid var(--line)',
              color: 'var(--ink)',
              boxShadow: 'var(--shadow-sm)',
            }),
```

변경:
```ts
            } : {
              background: hasSelfMention ? 'rgba(147,51,234,0.08)' : 'var(--bubble-in)',
              border: hasSelfMention ? '1px solid rgba(147,51,234,0.20)' : '1px solid var(--line)',
              color: 'var(--ink)',
              boxShadow: 'var(--shadow-sm)',
            }),
            ...(message.sender?.is_bot && !isOwn ? { borderLeft: '2px solid var(--brand)' } : {}),
```

> 이 라인은 기존 `isRequestPending` border-left 처리와 같은 패턴으로 추가. 정확한 위치는 기존 borderLeft spread 블록 바로 아래.

- [ ] **Step 4: 봇 메시지 DM방에서 발신자명 항상 표시**

봇 방은 group이 아닌 1:1 DM이라 `showSenderInfo=false`가 됨. 봇 메시지에는 이름을 표시해야 함.

`src/components/chat/MessageBubble.tsx`에서 발신자명 조건:

현재:
```tsx
{showSenderInfo && !isOwn && !isContinuation && message.sender && (
```

변경:
```tsx
{(showSenderInfo || message.sender?.is_bot) && !isOwn && !isContinuation && message.sender && (
```

- [ ] **Step 5: 빌드 검증**

```bash
npm run build
```

Expected: 성공. 타입 에러 없음.

- [ ] **Step 6: 커밋**

```bash
git add src/hooks/useMessages.ts src/components/layout/ChatWindow.tsx src/components/chat/MessageList.tsx src/components/ui/Avatar.tsx src/components/chat/MessageBubble.tsx
git commit -m "feat: bot typing indicator, avatar, and AI badge in MessageBubble"
```

---

## Task 9: 최종 빌드 검증 + 배포 준비

- [ ] **Step 1: 전체 빌드 검증**

```bash
npm run build
```

Expected: 빌드 성공, 타입 에러 0개

- [ ] **Step 2: 기존 기능 회귀 확인 포인트**

수동으로 확인:
1. 기존 DM 방 열기 → 메시지 전송 → 정상 동작
2. 채널 진입 → 멤버 목록 → 봇 미표시
3. MenuRail 봇 아이콘 → 봇 방 생성 → Chat 섹션 전환
4. 봇 방에서 메시지 → 타이핑 인디케이터 → 봇 응답 (API 키 설정 후)
5. Bot 아바타 + AI 뱃지 표시

- [ ] **Step 3: Supabase 시크릿 설정 (배포 환경)**

```bash
npx supabase secrets set ANTHROPIC_API_KEY=sk-ant-YOUR_KEY_HERE
```

- [ ] **Step 4: 최종 커밋 + 푸시**

```bash
git add -A
git commit -m "phase 봇: MTL 도우미 + Haiku API + 타이핑 인디케이터"
git push origin main
```

---

## 확인 체크리스트

### DB
- [ ] `SELECT is_bot, name FROM profiles WHERE is_bot = true` → 1 row 반환
- [ ] 봇이 #공지, #일반 채널에 미가입 확인

### 봇 방 라우팅
- [ ] MenuRail 봇 아이콘 opacity=1 (활성 스타일)
- [ ] 클릭 → 봇 1:1 대화방 생성 + 진입
- [ ] 두 번 클릭해도 중복 방 미생성

### 대화
- [ ] 메시지 전송 → "MTL 도우미가 입력 중..." 표시
- [ ] 5~15초 내 봇 응답 수신
- [ ] 봇 메시지: Bot 아바타 + AI 뱃지 + 좌측 brand border
- [ ] 봇 메시지 번역 미처리 (source_language = userLanguage)

### 회귀
- [ ] 기존 DM/채널 정상
- [ ] 빌드 통과
